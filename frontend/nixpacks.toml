[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install serve"]

[phases.build]
cmds = ["echo 'Skipping build — using pre-built dist/'"]

[start]
cmd = "npx serve -s dist -p ${PORT:-3000}"
